// Create a function adds that accepts one parameter and adds an "s" to it.
function adds(addToString) {
  // add an "s" to the input and return the result
  return addToString + "s";
}

console.log(adds("car"));
console.log(adds('house'));